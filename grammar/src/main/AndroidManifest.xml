<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grammar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VariableActivity" />
        <activity android:name=".ArrayActivity" />
        <activity android:name=".StringActivity" />
        <activity android:name=".SetOfActivity" />
        <activity android:name=".ListOfActivity" />
        <activity android:name=".MapOfActivity" />
        <activity android:name=".FunctionActivity" />
        <activity android:name=".ConditionActivity" />
        <activity android:name=".RepeatActivity" />
        <activity android:name=".NullActivity" />
        <activity android:name=".EqualActivity" />
        <activity android:name=".ParamActivity" />
        <activity android:name=".SpecialActivity" />
        <activity android:name=".SystemActivity" />
        <activity android:name=".ClassActivity" />
        <activity android:name=".MemberActivity" />
        <activity android:name=".InheritActivity" />
        <activity android:name=".SecretActivity" />
    </application>

</manifest>